/* export const getSession = () => {
  const session = sessionStorage.getItem('x-authorization');
  const local = localStorage.getItem('x-authorization');
  return session ? JSON.parse(session) : null, local ? JSON.parse(local) : null;
}; */