import { Component } from '@angular/core';

@Component({
    selector: 'sc-login',
    templateUrl: './login.component.html',
    standalone: true

})
export class LoginComponent{ 
    constructor(){ }
}